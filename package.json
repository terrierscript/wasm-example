{
  "name": "wasm-sample2",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "poi": "^9.6.1",
    "react-scripts": "1.0.17"
  },
  "dependencies": {
    "assemblyscript": "^0.3.0",
    "assemblyscript-loader": "^0.3.0",
    "react": "^16.2.0",
    "react-dom": "^16.2.0"
  },
  "scripts": {
    "start": "poi",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "prestart": "asc wasm/hello.ts -b > public/hello.wasm",
    "asc": "asc wasm/hello.ts  -b hello.wasm -t hello.wast -a hello.js"
  }
}
